1. Создайте новый модуль, например Training_Module1, включите его. Какой минимальный код требуется, чтобы сделать это?
- файы etc/module.xml и registration.php 
2.Сделайте намеренную ошибку в файле module.xml, ломающую структуру xml. Почистите кэш Magento. Что происходит при загрузке сайта?
- все сомалось, куча строк с описанием exception
3. Создайте второй модуль, например Training_Module2, создайте в обоих модулях файлы composer.json, в новом модуле пропишите зависимость от первого модуля в файле composer.json, включите второй модуль. Затем попытайтесь выключить первый модуль, опишите что происходит.
- Выкючить не поучается, ругается, т.к модуль2 зависит от модуля1. (Cannot disable Astrio_Training_Module1 because modules depend on it:
Astrio_Training_Module2: Astrio_Training_Module2->Astrio_Training_Module1)

4. Не поучилось сделать в модуле Astrio_Training_Module1 из за его названия, ругался тут 
<resource>Astrio_Test::test_config</resource> решила не переделывать, а создать новый - Test

5. Измените значения настроек через административный интерфейс, в таблице core_config_data найдите записи, в которых хранятся значения данных настроек.
- в таблице новые записи появились, все сработало.